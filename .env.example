# AcademicHub backend API base URL (defaults to production)
VITE_API_URL=https://api.academichubpro.com/api/v1

# Google Identity Services client ID — enables the "Continue with Google" button.
# The button is hidden when this is not set.
VITE_GOOGLE_CLIENT_ID=
